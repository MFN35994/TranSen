name: transen_auth
description: Authentication module for TranSen.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  firebase_core: ^4.6.0
  flutter:
    sdk: flutter
  transen_core:
    path: ../../core
  cloud_firestore: ^6.2.0
  flutter_riverpod: ^3.3.1
  mask_text_input_formatter: ^2.9.0
  dio: ^5.4.1
  shared_preferences: ^2.5.2
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
